using UnityEngine;

// ARModelLoader is disabled — ARSceneInitializer + ModelLoader handle all GLB loading in ARScene.
// Having both active causes the same file to be loaded twice simultaneously.
// If you need this class, remove ARSceneInitializer from the scene instead.
public class ARModelLoader : MonoBehaviour { }
